[project]
name = "elm-research"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.96.0",
    "jsonschema>=4.23.0",
    "python-dotenv>=1.0.1",
]

[dependency-groups]
dev = []
